apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
    - port: 80          # Port the service exposes
      targetPort: 80    # Port the container listens on
      nodePort: 30080   # Port exposed on your VM's IP